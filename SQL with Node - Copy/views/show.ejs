<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Users</title>
    <style>

      *{
        margin: 0;
        padding: 0;
        box-sizing: border-box;
      }

      body{
        display: flex;
        justify-content: center;
        align-items: center;
        flex-direction: column;
      }
      h1 {
        margin: 50px;
      }
      table,
      tr,
      th,
      td {
        border: 1px solid black;
      }

    </style>
  </head>
  <body>
    <h1>Users</h1>
    <table>
      <tr>
        <th>Id</th>
        <th>Username</th>
        <th>Email</th>
        <th>Edit</th>
      </tr>
      <% for(user of users) { %>
      <tr>
        <td><%= user.id %></td>
        <td><%= user.username %></td>
        <td><%= user.email %></td>
        <td><a href="http://localhost:3000/users/<%= user.id %>/edit">Edit</a></td>
      </tr>
      <% } %>
    </table>
  </body>
</html>
