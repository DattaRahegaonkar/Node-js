<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit your details</title>
    <style>
        body {
            display: flex;
            justify-content: center;
            align-items: center;
            flex-direction: column;
        }
    </style>
</head>
<body>
    <h1>Edit your information</h1>
    <form method="post" action="/data/<%= users[0].id %>?_method=PATCH">
        <h1>Email : <%= users[0].email %></h1>
        <textarea name="username" placeholder="Enter New Username"><%= users[0].username %></textarea><br><br>
        <textarea name="password" placeholder="Enter password" required></textarea> <br><br>
        <button>Edit</button>
    </form>
    <form action="/data">
        <button>Back</button>
    </form>
</body>
</html>